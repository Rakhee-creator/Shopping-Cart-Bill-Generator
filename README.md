# 🛒 Shopping Cart Bill Generator
A simple Java console-based program to simulate a shopping cart.  
This project demonstrates **loops, switch-case,if and user input handling**.
---
## ✨ Features
- Menu-driven item selection using `switch`.
- Allows multiple items and quantities until user chooses to exit.
- Accumulates total bill correctly.
- Clean exit option (`5`) to generate final bill.

## 📂 Project Structure
- `Shopping.java` → Main program with `do-while` loop and `switch-case`.

